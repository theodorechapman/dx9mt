# tests

Native smoke and packet-contract tests.

Run:
```sh
make -C dx9mt test-native
```

Current coverage:
- backend packet parser contract
- monotonic packet sequence enforcement
- present-target metadata requirement
- draw packet size and draw-state ID validation
